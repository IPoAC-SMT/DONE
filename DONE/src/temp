from scapy.all import *

packet = IP(dst="10.0.0.2") / TCP(
    sport=1234,  # Source port
    dport=8080,   # Destination port
    seq = 0,
    ack = 0,
    flags = "S",
    window = 2000,
    options=[('MSS', 3000)],
  
)

# Send the packet
send(packet)